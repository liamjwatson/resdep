To restore defaults:
1. Create an instance of epicsBLM (e.g. blm = epicsBLM())
2. Call restore_defaults() (e.g. blm.restore_defaults(mode='all'))
    2.a. modes are one of 'all', 'adc_counter_masks', and 'settings'

See epicsBLM class for more info -- code is documented.
